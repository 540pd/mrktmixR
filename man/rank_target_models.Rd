% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auxiliary_functions.R
\name{rank_target_models}
\alias{rank_target_models}
\title{Rank Target Models Based on Contribution Metrics}
\usage{
rank_target_models(
  data,
  stage_id_col,
  variable_col,
  adstock_col,
  power_col,
  lag_col,
  contri_min_col,
  contri_max_col
)
}
\arguments{
\item{data}{A data frame containing detailed model information including minimum and maximum contributions.}

\item{stage_id_col}{Name of the column in \code{data} representing the stage ID.}

\item{variable_col}{Name of the column in \code{data} representing the variable or factor.}

\item{adstock_col}{Name of the column in \code{data} representing the adstock effect.}

\item{power_col}{Name of the column in \code{data} representing the power of the model.}

\item{lag_col}{Name of the column in \code{data} representing the lag effect.}

\item{contri_min_col}{Name of the column in \code{data} representing the minimum contribution value.}

\item{contri_max_col}{Name of the column in \code{data} representing the maximum contribution value.}
}
\value{
A data frame with models ranked based on the specified criteria. The output data frame excludes the intermediate columns
used for calculations such as minimum contribution, contribution range, and count of models per group.
}
\description{
This function processes a data frame to rank models based on specified criteria related to marketing contributions.
It filters the data to include only relevant columns, calculates the range of contribution for each model, and ranks
the models based on multiple attributes including adstock, power, and lag.
}
\examples{
\dontrun{
data <- data.frame(
  stage_id = c(1, 1, 2, 2),
  variable = c("A", "B", "A", "B"),
  adstock = c(0.5, 0.3, 0.4, 0.6),
  power = c(2, 2, 3, 3),
  lag = c(1, 2, 1, 2),
  contri_min = c(10, 15, 10, 20),
  contri_max = c(20, 25, 15, 30)
)
rank_target_models(data, "stage_id", "variable", "adstock", "power", "lag", "contri_min", "contri_max")
}
}
